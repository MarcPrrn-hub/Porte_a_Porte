
import {parcours, param} from './parcours';
import {affichage} from './affichage';
import {initial_position, max_blocks, resultType, skins} from './initial_position'
const maze = {Parcours: parcours , Initial_pos: initial_position, Affichage: affichage,
              Param: param, Max_Blocks: max_blocks, Result: resultType, Skin: skins }

export default maze ;
